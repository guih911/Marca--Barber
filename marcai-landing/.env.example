# URL pública canónica do site da landing (sem barra no fim). Usada em OG, canonical, UTM e JSON-LD em runtime.
# No Docker, passe como build-arg (ver docker-compose) ou crie .env a partir deste ficheiro.
VITE_LANDING_BASE_URL=https://marcaí.com
